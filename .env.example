# Copiar a .env y ajustar. Ningún valor de aquí es secreto.
# El fichero .env real está en .gitignore y no debe subirse nunca.

# ---- Aplicación ----
APP_NAME=Dashboard de Proyectos

# ---- Autenticación HTTP Basic ----
# Actívala siempre que la app salga de localhost.
# Con ENABLE_AUTH=true la app se NIEGA A ARRANCAR si AUTH_PASSWORD sigue
# siendo el valor de fábrica ("changeme") o tiene menos de 8 caracteres.
ENABLE_AUTH=false
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# ---- Almacenamiento ----
# Rutas dentro del contenedor; docker-compose.yml las monta desde el host.
DB_PATH=/data/projects.db
BACKUP_KEEP=14

# ---- Puerto en el host (solo lo usa docker-compose) ----
HOST_PORT=8003

# ---- Repositorios locales ----
LOCAL_REPOS_BASE_PATH=/repos
# Niveles de carpeta que se recorren buscando repos. Con 1 solo se ven los repos
# que cuelgan directamente de LOCAL_REPOS_BASE_PATH: si los agrupas en
# subcarpetas ("Aplicaciones Servidor/mi-app"), necesitas 2 o 3.
PROJECTS_SCAN_DEPTH=3

# ---- Tokens de las APIs remotas (opcionales) ----
# Sin token se usan los límites públicos de cada API (bajos).
GITHUB_TOKEN=
GITLAB_TOKEN=
BITBUCKET_TOKEN=

# ---- Frecuencia de sincronización ----
# El ciclo local (git) es barato y frecuente; el remoto va espaciado por los
# límites de la API de GitHub. El descubrimiento recorre el disco, así que va
# aún más espaciado.
LOCAL_SYNC_MINUTES=15
REMOTE_SYNC_MINUTES=60
DISCOVERY_MINUTES=360

# ---- Alta automática ----
# Con GITHUB_TOKEN puesto, da de alta solos los repos de tu cuenta que no estén
# clonados en esta máquina (útil para el escaparate). Ponlo a false si solo
# quieres ver lo que tienes en local.
AUTO_IMPORT_GITHUB=true

# ---- Umbrales de estado ----
STALE_PROJECT_DAYS=30
STALE_PR_DAYS=7

# ---- Zona horaria (resumen diario y backups) ----
TIMEZONE=UTC

# ---- Avisos por Telegram (opcionales) ----
# Bot creado con @BotFather; el chat id te lo da @userinfobot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
